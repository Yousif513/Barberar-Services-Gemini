# Co-Working Changelog & Sync Registry

This file coordinates development between **Gemini (Antigravity)**, **Claude Code**, and **Codex/Cursor** to prevent file overwrites, regressions, and branch confusion.

---

## 1. Branching & Co-Working Protocol

To ensure models do not overwrite or conflict with each other's active work, we adhere to the following branch isolation guidelines:

### Model-Specific Branches
* **Claude Code**: Works on the **`claude-code`** branch.
* **Gemini (Antigravity)**: Works on the **`gemini`** branch.
* **Codex / Cursor (User Direct)**: Works on the branch of the active feature or `master`.

### Protocol Steps (Before starting any task)
1. **Check Branch (do NOT switch)**: Run `git status` to see the active branch. **Work on whatever branch is already checked out.** The human drives branch selection.
2. **Check for Uncommitted Changes**: Run `git status` to confirm there are no uncommitted changes from another model before modifying files. If there are, leave them untouched.
3. **Stay in sync via this changelog**: Read the latest entries below to see what other agents touched, and avoid heavy rewrites of files another agent changed recently.

### 🔒 Golden Rules (MANDATORY — these override the branch-per-agent guidance above)
> Added 2026-06-19 after a coworking incident: an agent switched branches (`git checkout`), which swapped the working tree to another branch's files and *looked* like work was deleted. Nothing was lost, but it disrupted the human. To prevent recurrence:
1. **NEVER switch the active branch or rewrite history.** Do not run `git checkout <branch>` / `git switch`, `git reset --hard`, `git rebase`, force-push, or cross-branch `git cherry-pick`. Branch management is the human's job — commit on the branch that is already checked out.
2. **NEVER overwrite another agent's files.** Use small, targeted edits. If a file appears in another agent's recent changelog entry, coordinate before touching it heavily.
3. **Commit on the active branch** with clear agent attribution and append a changelog entry. **Do not push another agent's branch** unless the human asks.
4. **To move work between branches, ASK THE HUMAN.** Never merge or cherry-pick across agent branches on your own.
5. **If you discover you are on a different branch than expected, STOP and tell the human — do not "fix" it by switching.**

---

## 2. Dev Change Log (Chronological)

### [2026-06-18] Gemini (Antigravity)
* **Tasks Completed**:
  * Configured three custom design system skills under `.agents/skills/` to elevate dashboard pages (`primora-micro-interactions`, `primora-responsive-density`, `primora-bilingual-rtl`).
  * Completely redesigned the Super Admin Layout ([layout.tsx](file:///c:/Users/Yousif's%20PC/Desktop/Ai%20Projects/Barberar%20Services-Gemini/web_platform/src/app/admin/layout.tsx)) with the Solid Obsidian Dark theme (`#111827`), grouped navigation section headers, bottom Administrator profile card, and translation sync.
  * Completely redesigned the main Admin Dashboard Page ([page.tsx](file:///c:/Users/Yousif's%20PC/Desktop/Ai%20Projects/Barberar%20Services-Gemini/web_platform/src/app/admin/page.tsx)) using a responsive 12-column grid. Created interactive custom SVG line/donut/sparkline visualizations, live platforms status feed, quick action button grids, platform insights metrics, and fully mirrored bilingual EN/AR layouts.
  * Verified build health: Next.js compilation succeeded and Expo React Native mobile typecheck passed with zero errors.
* **Active Working Commit**: `b6efd33f31c4f82e919cf06f00e54818f75081c7`
* **Next Steps**:
  * Handover to Claude Code or Codex for further page redesigns (e.g., Disputes, Providers, Ledger) using the newly established design tokens and layout systems.


### [2026-06-18] Gemini (Antigravity) - Part 2
* **Tasks Completed**:
  * Redesigned Bookings Ledger ([bookings/page.tsx](file:///c:/Users/Yousif's%20PC/Desktop/Ai%20Projects/Barberar%20Services-Gemini/web_platform/src/app/admin/bookings/page.tsx)) and Ledger Splits ([ledger/page.tsx](file:///c:/Users/Yousif's%20PC/Desktop/Ai%20Projects/Barberar%20Services-Gemini/web_platform/src/app/admin/ledger/page.tsx)) with top split metrics (Platform Revenue, Active Count, Gross Captured, Provider Share), payouts release triggers, and dark obsidian / gold accent themes.
  * Redesigned Disputes Arbitrator Page ([disputes/page.tsx](file:///c:/Users/Yousif's%20PC/Desktop/Ai%20Projects/Barberar%20Services-Gemini/web_platform/src/app/admin/disputes/page.tsx)) with top KPI metrics (Total Disputes, Pending Disputes, Refunded Total), polished disputes list card layouts, styled stars rating, verified buttons, actions, and Arabic translation support.
  * Redesigned Provider Registry Page ([providers/page.tsx](file:///c:/Users/Yousif's%20PC/Desktop/Ai%20Projects/Barberar%20Services-Gemini/web_platform/src/app/admin/providers/page.tsx)) with top KPI metrics (Total Providers, Verified Businesses, Average Commission), styled provider row controls, commission update field, trade license link, verification action button, and Arabic translation support.
  * Mapped payouts sidebar option to the ledger page in layout ([layout.tsx](file:///c:/Users/Yousif's%20PC/Desktop/Ai%20Projects/Barberar%20Services-Gemini/web_platform/src/app/admin/layout.tsx)).
  * Verified build health: Next.js compilation succeeded and Expo React Native mobile typecheck passed with zero errors.
* **Active Working Commit**: a926fd5b0639e76925002db9d4b0eca979e854d4
* **Next Steps**:
  * Handover to other agents / models to redesign management & system views.


### [2026-06-18] Gemini (Antigravity) - Part 3
* **Tasks Completed**:
  * Implemented and completed all 21 remaining admin dashboard subpages under `web_platform/src/app/admin/` (including Analytics, Customers, Services, Packages, Orders, Payments, Commissions, Reviews, Reports, Teams, Roles, Branches, Rooms, Locations, Taxes, Coupons, Integrations, Webhooks, System Logs, Audit Logs, and Settings).
  * Styled each page using the Primora premium dark luxury SaaS layout standards, full bilingual (EN/AR) translation sets, and automatic RTL direction mirroring.
  * Verified build health: Next.js compilation succeeded and Expo React Native mobile typecheck passed with zero errors.
* **Active Working Commit**: `d07ab0fb806e2266cf8d5a174f990750863abf8b`
* **Next Steps**:
  * Handover to other agents / models to review the newly created views.

---

### [2026-06-18] Codex
* **Tasks Completed**:
  * Read and confirmed `AGENTS.md`, `.cursorrules`, and `.coworking_changelog.md` coworking protocol.
  * Created/switched local `codex` branch from `origin/Codex` and fast-forwarded it with `origin/master` to `da8476b`.
  * No product code files were modified in this sync/readiness pass.
* **Files Changed**: `.coworking_changelog.md`
* **Active Working Commit Before This Log Entry**: `da8476b`
* **Next Steps**:
  * Review the newly added admin views from Gemini on `master`.
  * Coordinate with Claude Code before porting later provider Phase 3 work from `claude-code`.
  * Continue provider Phase 3 only with targeted edits: deposit enforcer, multi-branch calendar availability CRUD, wallet/cash ledger wiring.

---

---

### [2026-06-18] Codex - Customer Dashboard Luxury Redesign
* **Tasks Completed**:
  * Redesigned the customer dashboard page to match the provided PRIMORA luxury AI dashboard reference: dark appointment hero, AI match chip, countdown panel, neural gold accents, glass panels, recommended service badges, loyalty glow, spending insight chip, and AI recommendation banner.
  * Updated the customer layout/sidebar to the dark PRIMORA sidebar treatment with gold navigation states, neural ambient decoration, AI assistant card, gold member footer, and hidden scrollbars.
  * Verified local dashboard render in the in-app browser at `http://localhost:3000/customer/dashboard`; no browser console errors were present.
  * Verified build health: `npm run typecheck:mobile` and `npm run build --workspace=web_platform` both passed.
* **Files Changed**:
  * `web_platform/src/app/customer/dashboard/page.tsx`
  * `web_platform/src/app/customer/layout.tsx`
* **Product Commit**: `ebaed34`
* **Next Steps**:
  * Apply the same PRIMORA customer-dashboard visual system to deeper customer pages such as Bookings, Wallet, Favorites, Messages, and Settings.
  * Keep customer/admin/provider Supabase/Vercel project boundaries separate; this change only touched Barberar customer UI files.

---

### [2026-06-18] Codex - Customer Dashboard Glow Refinement
* **Tasks Completed**:
  * Intensified the PRIMORA customer dashboard glow treatment using stronger champagne-gold borders, luminous multi-layer shadows, and brighter neural mesh overlays.
  * Added reusable neural mesh rendering to the customer dashboard and applied it across the page background, appointment hero, offer card, white content cards, wallet/insights cards, and AI recommendation bar.
  * Strengthened the customer sidebar border, active navigation glow, assistant card shine, and background net so the layout tracks closer to the provided screenshot.
  * Verified local dashboard render in the in-app browser at `http://localhost:3000/customer/dashboard`; no browser console errors were present.
  * Verified build health: `npm run typecheck:mobile` and `npm run build --workspace=web_platform` both passed.
* **Files Changed**:
  * `web_platform/src/app/customer/dashboard/page.tsx`
  * `web_platform/src/app/customer/layout.tsx`
* **Product Commit**: `771684c`
* **Next Steps**:
  * Review visual density on smaller laptop heights after applying similar glow language to the deeper customer pages.

---

### [2026-06-18] Codex - Customer Dashboard Corner And Mesh Glow Fix
* **Tasks Completed**:
  * Removed the active sidebar item's vertical gold stripe while preserving the rounded gold active pill.
  * Reworked card corner glow from blocky corner overlays into thin champagne-gold corner strokes and glowing corner dots.
  * Added SVG glow filters and halo nodes to neural mesh connections so intersection points shine closer to the provided reference image.
  * Verified local dashboard render in the in-app browser at `http://localhost:3000/customer/dashboard`; no browser console errors were present.
  * Verified build health: `npm run typecheck:mobile` and `npm run build --workspace=web_platform` both passed.
* **Files Changed**:
  * `web_platform/src/app/customer/dashboard/page.tsx`
  * `web_platform/src/app/customer/layout.tsx`
* **Product Commit**: `57b5a61`
* **Next Steps**:
  * Continue applying the same refined corner and neural mesh language to deeper customer dashboard pages.

---

### [2026-06-19] Codex - Admin And Provider Dashboard Luxury Skin
* **Tasks Completed**:
  * Added a shared PRIMORA dashboard skin in `web_platform/src/app/globals.css` with warm ivory background gradients, champagne-gold card borders, corner glow strokes, neural mesh overlays, active nav glow, and dark luxury sidebar treatment.
  * Applied the shared skin to `web_platform/src/app/admin/layout.tsx` so all admin dashboard pages inherit the customer-dashboard visual language while preserving the existing grouped navigation and page layout.
  * Applied the shared skin to `web_platform/src/app/provider/layout.tsx`, preserving the provider tabs/layout while replacing the old light sidebar with the PRIMORA dark sidebar, gold active states, and matching support/footer styling.
  * Verified `npm run typecheck:mobile` and `npm run build --workspace=web_platform` both passed.
  * Swept 45 admin/provider routes in the in-app browser after hydration; dashboard routes rendered with the shared skin and no browser console errors. `/provider/become` redirects to the public `/become-provider` page and is intentionally outside the provider dashboard shell.
* **Files Changed**:
  * `web_platform/src/app/globals.css`
  * `web_platform/src/app/admin/layout.tsx`
  * `web_platform/src/app/provider/layout.tsx`
* **Product Commit**: `cbb9f9a`
* **Next Steps**:
  * Visual QA pass on a full desktop monitor to tune individual chart/table density where needed.
  * Continue avoiding full-file rewrites so Gemini/Codex/other-agent page work remains merge-friendly.

---

### [2026-06-19] Codex - Landing Page PRIMORA Theme Refresh
* **Tasks Completed**:
  * Restyled the public landing page to match the PRIMORA dashboard spirit with a calmer warm ivory surface, obsidian header/footer accents, champagne-gold borders, refined card shadows, and premium grooming photography.
  * Reworked the hero into an editorial landing composition with a soft glass panel, subtle gold line texture, dark featured grooming card, and restrained premium CTAs rather than dashboard-level glow.
  * Updated the value proposition strip, category cards, featured artist cards, how-it-works cards, provider CTA, and footer colors to align with the dashboard theme while keeping the existing bilingual content and layout structure.
  * Verified `npm run typecheck:mobile` and `npm run build --workspace=web_platform` both passed.
  * Previewed `http://localhost:3000/` in the in-app browser; the page rendered with the new theme and no browser console errors.
* **Files Changed**:
  * `web_platform/src/app/page.tsx`
* **Product Commit**: `13c3c35`
* **Next Steps**:
  * Consider applying the same calmer public-site theme to About, Store, Service Board, and category pages for full public-brand consistency.

---

### [2026-06-19] Codex - Admin Sidebar Provider Match
* **Tasks Completed**:
  * Updated `web_platform/src/app/admin/layout.tsx` so the admin sidebar now uses the same floating outer padding, rounded dark PRIMORA panel, width, gold border, active nav glow, and footer treatment as the provider sidebar.
  * Preserved the admin grouped navigation sections, admin routes, active route behavior, RTL row switching, and admin role/profile semantics.
  * Added bilingual labels for the new admin sidebar support/profile footer content.
  * Verified `npm run typecheck:mobile` and `npm run build --workspace=web_platform` both passed.
  * Previewed `http://localhost:3000/admin` in the in-app browser; the sidebar rendered with provider-style classes and no browser console errors.
* **Files Changed**:
  * `web_platform/src/app/admin/layout.tsx`
* **Product Commit**: `f99a6f6`
* **Next Steps**:
  * Visual QA on a taller desktop viewport to decide whether admin grouped nav spacing should be slightly denser than provider because admin has more links.

---

### [2026-06-19] Claude Code - Part 5 (Deposit Enforcer)
* **Tasks Completed**:
  * **Phase 3 feature — Deposit Enforcer**. Backend migration `20260619063540_provider_deposit_enforcer.sql` (pushed + verified on linked Supabase): added `providers.deposit_percentage` (DECIMAL, default 20, CHECK 0–100) and recreated `create_booking` to compute `deposit_required` from the provider's `deposit_percentage` instead of the hardcoded 15% (tax stays 15% VAT, commission stays provider-configured).
  * UI: new **"Booking Deposit Policy"** section in `provider/settings` — slider (0–100%) + live "on a 200 SAR service" preview + save to `providers` (uses existing "Owners manage own providers" RLS; protect trigger leaves the new column writable).
  * Verified: `npm run build --workspace=web_platform` passes (EXIT 0); remote `has_col=1`, `create_booking` uses `v_deposit_pct`.
* **Files Changed**: `supabase/migrations/20260619063540_provider_deposit_enforcer.sql`, `web_platform/src/app/provider/settings/page.tsx`
* **Active Working Commit**: `5fa7283` (branch `claude-code`)
* **Heads-up for the team**: `provider/pricing/page.tsx` line ~938 footer reverted to `bg-white border-gray-200` (white footer on dark page) after a concurrent edit — needs re-darkening.
* **Next Steps**:
  * Continue Phase 3: multi-branch calendar (`employee_availability` CRUD) and wallet/cash ledger (`transactional_ledger`).

### [2026-06-19] Claude Code - Part 6 (Coworking incident fix + Golden Rules)
* **What happened**: While working, the active branch had been switched to `codex` (by another tool). I committed there, then "corrected" by `git checkout claude-code` — which swapped the working tree and made it look like Codex's recent work (landing-page luxury restyle, sidebar matches, etc.) was lost. **Nothing was deleted** — it was all safe in git on `codex`/`claude-code`.
* **Fix applied**: Switched back to `codex` (the human's active branch) so all their work returned, with my Deposit Enforcer included (it's at `5fa7283` on `codex`). My other accomplishments remain safe on `claude-code` (origin `ae8b33b`) and can be brought over only if the human asks.
* **Rules updated (shared with all agents)**: Added **🔒 Golden Rules** to `.cursorrules` (§5) and this file (§1): never switch branches / rewrite history / cross-branch cherry-pick; never overwrite another agent's files; commit on the active branch; move work between branches only with the human's go-ahead; if on an unexpected branch, STOP and tell the human.
* **Files Changed**: `.cursorrules`, `.coworking_changelog.md`
* **Active Branch**: `codex` (not pushed — left for the human to manage).
* **Next Steps**: Resume Phase 3 on whatever branch the human keeps active; do not switch.

---

### [2026-06-20] Codex - Dashboard CRUD Controls And Schema Fixes
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches or merge `master` because the Golden Rules now make branch movement human-owned.
  * Left untracked `.agents/` untouched.
* **Tasks Completed**:
  * Fixed admin service management to use real `services` schema columns (`base_price`, `base_duration_minutes`, `is_active`) and persist price/status edits through Supabase.
  * Fixed customer settings to use `profiles.phone_number` and `client_profiles` for dependents instead of non-existent `phone` / `dependents` schema paths.
  * Fixed customer wallet to read `transactional_ledger` joined to customer bookings instead of the non-existent `ledger_entries` table; removed fake starting balances and added clear notices for top-up/card actions that are not enabled yet.
  * Fixed provider settings phone persistence by splitting provider business fields from `profiles.phone_number`.
  * Wired provider Team page Add/Edit/Delete, service assignment, and weekly shift editing to `employees`, `employee_services`, and `employee_availability`.
  * Wired provider Services page Add/Edit/Delete to real `services` rows and active `categories`, preserving the existing table/card layout.
  * Added migration `20260620123831_admin_manage_provider_services.sql` for admin service moderation policy and explicit Data API grants for dashboard CRUD tables.
* **Files Changed**:
  * `supabase/migrations/20260620123831_admin_manage_provider_services.sql`
  * `web_platform/src/app/admin/services/page.tsx`
  * `web_platform/src/app/customer/settings/page.tsx`
  * `web_platform/src/app/customer/wallet/page.tsx`
  * `web_platform/src/app/provider/services/page.tsx`
  * `web_platform/src/app/provider/settings/page.tsx`
  * `web_platform/src/app/provider/team/page.tsx`
* **Verification**:
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed.
  * In-app browser smoke check for `/provider/services` and `/provider/team` showed the secure-session gate with no fresh console errors.
  * `npx supabase migration list --local` could not connect because local Supabase Postgres at `127.0.0.1:54322` was not running.
* **Active Working Commit**: `40b00af`
* **Next Steps**:
  * Add a dev-only auth path or seeded local login workflow so development is not blocked by production email verification.
  * Continue replacing mock fallback pages with real empty states and Supabase-backed CRUD, starting with provider packages/resources/wallet and admin reports/settings.
  * Safely clean old encoded sample blocks in `provider/team` and `provider/services` once an encoding-aware patch can be coordinated.
  * Build end-to-end Playwright tests for provider service CRUD, staff CRUD, customer settings/dependents, and admin service moderation.

---

### [2026-06-20] Codex - Local Dev Auth Access
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Left concurrent unstaged provider dashboard edits (`provider/calendar`, `provider/pricing`, `provider/wallet`) and untracked `.agents/` untouched.
* **Tasks Completed**:
  * Made localhost development access independent from Supabase email verification so customer/provider/admin dashboards can be opened while real email confirmation is configured later.
  * Fixed the login page hydration path so the local access panel appears after the client loads instead of staying hidden behind the server snapshot.
  * Updated auth error and signup messages to point developers toward the local access buttons when confirmation blocks real auth.
  * Updated `.env.example` to document the localhost-only development role switcher.
  * Restarted the local web server in dev mode on `http://localhost:3000` so the in-app browser receives fresh code and hot reload works.
* **Files Changed**:
  * `.env.example`
  * `web_platform/src/app/login/page.tsx`
  * `web_platform/src/lib/dev-access.ts`
* **Verification**:
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check -- .env.example web_platform/src/app/login/page.tsx web_platform/src/lib/dev-access.ts` passed.
  * In-app browser smoke check confirmed the login page shows the local development access buttons and `/customer/dashboard` loads without the verification gate.
* **Product Commit**: `345c0db`
* **Next Steps**:
  * Continue Supabase-backed CRUD completion for provider packages/resources/wallet and admin reports/settings.
  * Add a small seeded demo-data/dev-auth guide so future agents can test role dashboards consistently without production email delivery.
  * Coordinate before staging the existing provider calendar/pricing/wallet workspace edits.

---

### [2026-06-20] Gemini (Antigravity)
* **Tasks Completed**:
  * Redesigned and fully wired the Working Hours Roster panel in `web_platform/src/app/provider/calendar/page.tsx` with a day-of-week selector dropdown, active toggle switch, start/end time selects, and a "Save Working Hours" button wired to `handleSaveShift`.
  * Redesigned and wired the Walk-in booking modal inputs with select elements populated by live customers (`profiles` table), services, and stylists (`employees` table) data.
  * Added time conversion utilities (`timeTo12Hour` and `timeTo24Hour`) to `calendar/page.tsx`.
  * Configured calendar `useEffect` dependency array so that changing `selectedDayToEdit` immediately triggers loading schedule/shift data.
  * Updated `handleBookingSubmit` to use `bookStaff` (the selected stylist from the modal) instead of calendar's `selectedEmployeeId` state.
  * Verified build health: `npm run typecheck:mobile` and `npm run build --workspace=web_platform` both compiled successfully with zero errors.
* **Files Changed**:
  * `web_platform/src/app/provider/calendar/page.tsx`
* **Active Working Commit**: `2992e39`
* **Next Steps**:
  * Implement dynamic time slot intervals based on selected service base duration rather than fixed 1-hour slots.
  * Implement double-booking checks or warnings when booking overlap is detected on the client-side.

---

### [2026-06-20] Codex - Provider Wallet Payout Requests
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Left untracked `.agents/` untouched.
* **Tasks Completed**:
  * Added `public.payout_requests` with provider ownership, requester tracking, bank/IBAN fields, status lifecycle, indexes, RLS, and Data API grants.
  * Added a follow-up policy refinement migration to avoid overlapping payout request `SELECT`/`INSERT` RLS policies flagged by Supabase advisors.
  * Wired `web_platform/src/app/provider/wallet/page.tsx` payout modal to insert real `payout_requests` rows instead of only changing local React state.
  * Added a payout request history table and recalculated withdrawable balance from pending ledger funds minus open payout requests.
  * Removed fake provider wallet starting balances and mock fallback ledger rows from the live wallet page.
  * Pushed pending Supabase migrations to linked Barberar project `vpszcnxsgmoavkqorjzt` (`20260620123831`, `20260620181924`, `20260620182904` now remote).
* **Files Changed**:
  * `supabase/migrations/20260620181924_provider_payout_requests.sql`
  * `supabase/migrations/20260620182904_refine_payout_request_policies.sql`
  * `web_platform/src/app/provider/wallet/page.tsx`
* **Verification**:
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed.
  * `npx supabase migration list --linked` confirmed local and remote migrations match through `20260620182904`.
  * `npx supabase db query --linked` confirmed `public.payout_requests` exists with RLS enabled and 4 policies.
  * `npx supabase db advisors --linked | Select-String -Pattern "payout_requests"` returned no payout-specific warnings after policy refinement.
  * In-app browser smoke check for `/provider/wallet` confirmed the payout request section renders, the verification gate is absent, and no fresh console errors were emitted.
* **Product Commit**: `aea3edf`
* **Next Steps**:
  * Add admin-side payout request review controls to approve/reject requests and release matching `transactional_ledger` rows.
  * Continue removing mock-only admin report/settings actions by backing them with Supabase data or generated exports.
  * Add role-authenticated test coverage for provider payout request creation and admin payout review.

---

### [2026-06-21] Codex - Admin Payout Request Review
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Left untracked `.agents/` untouched.
* **Tasks Completed**:
  * Added an admin payout request review panel to `web_platform/src/app/admin/ledger/page.tsx`.
  * Admins can now move provider payout requests to processing, reject them, or mark them paid.
  * Marking a request paid releases the provider's pending `transactional_ledger` rows oldest-first and records an admin note on the payout request.
  * Removed mock fallback Tap ledger rows from the admin ledger so empty Supabase data shows a real empty state instead of fake release controls.
  * Added bilingual labels and RTL-aware table/action layout for the payout request review panel.
* **Files Changed**:
  * `web_platform/src/app/admin/ledger/page.tsx`
* **Verification**:
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed.
  * `npx supabase migration list --linked` confirmed local and remote Barberar migrations still match through `20260620182904`.
  * `npx supabase db query --linked "select count(*) as payout_request_count from public.payout_requests;"` confirmed the linked Barberar table is reachable.
  * In-app browser smoke check for `/admin/ledger` confirmed the payout request panel and empty states render, fake Tap rows are gone, the verification gate is absent, and no fresh console errors were emitted.
* **Product Commit**: `d448d56`
* **Next Steps**:
  * Add a proper admin payout audit trail/report export for paid and rejected payout requests.
  * Replace admin payments/refund mock actions with real payment gateway state once Tap/Moyasar integration keys and refund flow are finalized.
  * Add automated role tests for provider payout request creation plus admin processing/rejection/paid transitions.

---

### [2026-06-21] Codex - Admin Payments Ledger Backing
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Left untracked `.agents/` untouched.
* **Tasks Completed**:
  * Replaced admin payments mock rows and hardcoded revenue cards with live `transactional_ledger` reads.
  * Added real loading and empty states so zero linked Supabase payment records display honestly instead of fake charges.
  * Updated payment summary cards to calculate gross captured, platform share, and provider share from ledger totals.
  * Changed the fake refund mutation into a manual gateway-review notice so the UI no longer marks funds refunded without Tap/Moyasar refund integration.
  * Added bilingual labels for the ledger-backed payment view and preserved RTL layout behavior.
* **Files Changed**:
  * `web_platform/src/app/admin/payments/page.tsx`
* **Verification**:
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed.
  * `npx supabase db query --linked` confirmed `public.transactional_ledger` is reachable on the linked Barberar project and currently has 0 rows.
  * In-app browser smoke check for `/admin/payments` confirmed the real empty state and zero totals render, fake charge rows are gone, and no console errors were emitted.
* **Product Commit**: `53ec9e5`
* **Next Steps**:
  * Add a dedicated refund request/audit table once Tap/Moyasar refund credentials and provider refund policy are finalized.
  * Replace remaining admin mock-only action buttons with either real Supabase mutations or honest disabled/manual-review states.
  * Add automated admin payment page tests covering empty ledger, populated ledger, and refund unavailable state.

---

### [2026-06-18] Claude Code
* **Tasks Completed**:
  * **Restored Gemini's Super Admin work as the base on `claude-code`**: brought the obsidian-dark sidebar layout + 1088-line detailed dashboard + redesigned bookings / disputes / ledger / providers subpages (Gemini `b6efd33`) into `claude-code`, replacing my earlier warm-sand single-screen admin rebuild. Verified it renders (dark `#111827` sidebar, detailed panels) with no compile errors. Future admin work will build on Gemini's version, not overwrite it.
  * Context (already on `master`/`claude-code` from prior runs): customer dashboard single-screen redesign + `profiles.gender` migration (`20260617232816`, pushed to Supabase) + search-result click fix; provider dashboard single-screen rebuild.
* **Files Changed**: `web_platform/src/app/admin/{layout,page,bookings/page,disputes/page,ledger/page,providers/page}.tsx`
* **Active Working Commit**: `d27258c` (branch `claude-code`)
* **Next Steps**:
  * Build new admin features on top of Gemini's restored dashboard (do not overwrite).
  * Reskin the **Provider dashboard** to Obsidian Dark `#070B12` per design-token §2 (currently warm sand).
  * Confirm with the team whether customer/provider dashboards keep the single-screen layout.

### [2026-06-18] Claude Code - Part 2
* **Tasks Completed**:
  * Synced `claude-code` with `master` — merged in Gemini's **21 new admin subpages** (`d07ab0f`) cleanly (no conflicts; changelog auto-merged).
  * **Reskinned the Provider portal to Obsidian Dark `#070B12`** per design-token §2: `provider/dashboard/page.tsx` and `provider/layout.tsx` (page `#070B12`, cards `#0F1623`, inner panels `#131C2B`, sidebar `#0B0F17`, light text, gold accents + avatar-on-gold preserved). Verified: `npm run build --workspace=web_platform` passes (EXIT 0), renders single-screen dark with no errors.
* **Files Changed**: `web_platform/src/app/provider/{dashboard/page,layout}.tsx`
* **Active Working Commit**: `53812a0` (branch `claude-code`)
* **Next Steps**:
  * Roll Obsidian Dark across the remaining **provider subpages** (services, calendar, pricing, team/staff-management, wallet, reports, etc.) so content matches the now-dark sidebar.
  * Then begin Phase 3 provider features (Service Menu Builder, multi-branch calendar, deposit enforcer, wallet ledger) per the completion roadmap.

### [2026-06-18] Claude Code - Part 3
* **Tasks Completed**:
  * Audited all 18 provider subpages: found they were **already premium obsidian-dark** (glassmorphism: page `#070B12`, cards `#111827`/`#0D1422`, panels `#172033`, text `#B8C0D4`/`#7B859C`, gold/green/amber accents) — likely from a prior Gemini/Codex pass. `text-[#101828]` dark-on-dark = 0 across all of them.
  * The only genuine light leftovers were **2 stray white footers** (`provider/pricing`, `provider/staff-management`) — fixed `bg-white border-gray-200` → `bg-[#0B0F17] border-white/[0.06]`. Build passes (EXIT 0).
* **Files Changed**: `web_platform/src/app/provider/{pricing,staff-management}/page.tsx`
* **Active Working Commit**: `76707b7` (branch `claude-code`)
* **Result**: Provider portal (dashboard + layout + 18 subpages) is now fully Obsidian Dark and build-clean.
* **Next Steps**:
  * Begin **Phase 3 provider features** (wire real data): Service Menu Builder (`services` CRUD), multi-branch calendar (`employee_availability`), deposit enforcer (feeds `create_booking`), wallet/cash ledger (`transactional_ledger`).

### [2026-06-18] Claude Code - Part 4
* **Tasks Completed**:
  * **Phase 3 started — Service Menu Builder wired to live Supabase** (`provider/services/page.tsx`). Kept the existing dark glassmorphic design; added: fetch of the owner's provider + their `services` (with `categories` join) + active categories; **Add/Edit** via a dark modal (name EN/AR, category select, base price, duration, home-eligible + active toggles); **Delete** with confirm. Falls back to showcase data when unauthenticated.
  * Verified RLS pre-exists ("Owners manage services" `FOR ALL`, owner-scoped) — no migration needed. Web build passes (EXIT 0).
  * Note: full CRUD round-trip needs a real provider login; dev-access preview shows fallback (no Supabase session), so verification was via build + RLS audit + code review.
* **Files Changed**: `web_platform/src/app/provider/services/page.tsx`
* **Active Working Commit**: `06d292c` (branch `claude-code`)
* **Next Steps**:
  * Continue Phase 3: **Deposit Enforcer** (provider/pricing or settings → feeds `create_booking` deposit %), then **multi-branch calendar** (`employee_availability` CRUD) and **wallet/cash ledger** wiring.

### [2026-06-19] Claude Code - Part 5 (Deposit Enforcer)
* **Tasks Completed**:
  * **Deposit Enforcer** delivered. Migration `20260619063540_provider_deposit_enforcer.sql` (pushed + verified on linked Supabase): added `providers.deposit_percentage` (DECIMAL, default 20, CHECK 0–100) and recreated `create_booking` to compute `deposit_required` from it instead of the hardcoded 15% (tax stays 15% VAT, commission stays provider-configured).
  * UI: new **"Booking Deposit Policy"** section in `provider/settings` — 0–100% slider + live "on a 200 SAR service" preview + save to `providers` (existing "Owners manage own providers" RLS; protect trigger leaves the new column writable).
  * Verified: web build passes (EXIT 0); remote `has_col=1`, `create_booking` uses `v_deposit_pct`.
  * Branch note: feature was first committed on `codex` by mistake (branch had been switched); cherry-picked onto `claude-code` (`4bea72a`). Stray duplicate commits remain on local `codex` only (not pushed).
* **Files Changed**: `supabase/migrations/20260619063540_provider_deposit_enforcer.sql`, `web_platform/src/app/provider/settings/page.tsx`
* **Active Working Commit**: `4bea72a` (branch `claude-code`)
* **Heads-up**: `provider/pricing/page.tsx` ~line 938 footer reverted to `bg-white border-gray-200` (white on dark) after a concurrent edit — needs re-darkening.
* **Next Steps**:
  * Continue Phase 3: multi-branch calendar (`employee_availability` CRUD), then wallet/cash ledger (`transactional_ledger`).

---

### [2026-06-21] Codex - Admin Controls + Supabase Rectification
* **Tasks Completed**:
  * Synced `codex` with `origin/master` and resolved provider merge conflicts without full-file overwrites.
  * Wired `admin/branches` to live Supabase `branches` data, replacing showcase branch rows and adding real `is_active` toggling.
  * Added live `promotional_codes` support for `admin/coupons` with add, edit, delete, and active/inactive toggles.
  * Added `payment_refund_requests` and changed `admin/payments` refund action from fake local success to a real manual-review request/audit record.
  * Added Barberar-only development data guidance in `docs/barberar-dev-dashboard-data.md` to avoid Chocolux/Supabase/Vercel mixing.
  * Added `scripts/verify-admin-controls.mjs` and included it in `npm run test:quality`.
  * Applied Supabase migrations to the linked Barberar project:
    * `20260621183407_admin_branch_coupon_controls.sql`
    * `20260621184453_refine_admin_control_policies.sql`
    * `20260621214926_refine_branch_mutation_policies.sql`
  * Refined branch RLS policies so branch mutations use one owner-or-admin policy per action, preserving public branch reads.
* **Files Changed**:
  * `web_platform/src/app/admin/branches/page.tsx`
  * `web_platform/src/app/admin/coupons/page.tsx`
  * `web_platform/src/app/admin/payments/page.tsx`
  * `supabase/migrations/20260621183407_admin_branch_coupon_controls.sql`
  * `supabase/migrations/20260621184453_refine_admin_control_policies.sql`
  * `supabase/migrations/20260621214926_refine_branch_mutation_policies.sql`
  * `scripts/verify-admin-controls.mjs`
  * `docs/barberar-dev-dashboard-data.md`
  * `package.json`
* **Verification**:
  * `npm run test:admin-controls` passed.
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run test:security-core` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed, with CRLF warnings only.
  * Linked Supabase queries confirmed `branches.is_active`, `promotional_codes`, and `payment_refund_requests` exist remotely.
  * Supabase advisor filter is clean for `branches`, `promotional_codes`, and `payment_refund_requests`.
  * In-app browser reached the secure-session guard on admin routes because no admin browser session was active; no fake data markers were present in guarded page text.
* **Commits**:
  * Sync merge from master: `0be1442`
  * Product implementation: `8e4812b`
* **Next Steps**:
  * Add real admin CRUD flows for bookings, providers, customers, packages, teams, rooms, taxes, webhooks, and integrations.
  * Connect refund processing to the chosen payment gateway once Tap/Moyasar credentials and policy are finalized.
  * Add authenticated E2E coverage with seeded admin/provider/customer accounts so guarded dashboard routes can be smoke-tested beyond the session guard.
  * Continue replacing remaining admin mock action buttons with either real Supabase mutations or honest disabled/manual-review states.

---

### [2026-06-23] Codex - Dashboard Visual Defect Pass
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Ran `git pull origin master`; branch was already up to date.
  * Project-local PRIMORA skill files referenced by `AGENTS.md` were not present in this checkout, so the pass used the installed/global rules plus the current dashboard implementation patterns.
* **Tasks Completed**:
  * Removed the provider sidebar active-item vertical gold strip so active navigation uses the rounded glowing card treatment only.
  * Removed the similar selected-message vertical strip in provider messages.
  * Repaired provider dark-theme defects where deliveries, settings, and staff-management headers still used light borders/text/backgrounds.
  * Fixed visible Arabic mojibake in customer wallet action notices and the admin ledger payout currency label.
* **Files Changed**:
  * `web_platform/src/app/provider/layout.tsx`
  * `web_platform/src/app/provider/messages/page.tsx`
  * `web_platform/src/app/provider/deliveries/page.tsx`
  * `web_platform/src/app/provider/settings/page.tsx`
  * `web_platform/src/app/provider/staff-management/page.tsx`
  * `web_platform/src/app/customer/wallet/page.tsx`
  * `web_platform/src/app/admin/ledger/page.tsx`
* **Verification**:
  * `rg` scans confirmed no remaining provider vertical-strip signatures, no targeted light-theme provider header tokens, and no dashboard mojibake signatures in admin/customer/provider TSX.
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run test:admin-controls` passed.
  * `npm run test:security-core` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed, with CRLF warnings only.
  * In-app browser smoke rendered provider, customer, and admin dashboard routes with no mojibake or vertical-strip classes detected. Browser dev logs still showed stale provider-layout errors from an older dev-server state, while current file scans and production build were clean.
* **Product Commit**: `0a6d993`
* **Next Steps**:
  * Continue a route-by-route authenticated visual QA pass for the remaining admin/customer/provider pages.
  * Add screenshot-based regression checks for sidebar active states, Arabic labels, and provider dark-theme headers.
  * Keep replacing remaining mock-only admin/customer/provider actions with real Supabase mutations or explicit manual-review states.

---

### [2026-06-23] Codex - Provider Black Rectangle Surface Fix
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Attempted `git pull origin master` twice, but GitHub HTTPS timed out both times before a connection could be established.
  * Project-local PRIMORA skill files were still not present in the workspace/user skill folders, so this used the current PRIMORA dashboard patterns already in code.
* **Tasks Completed**:
  * Removed the flat black rectangle from the provider dashboard page by replacing the full `#070B12` canvas with a rounded dark-gold glass gradient panel.
  * Replaced the provider sidebar/menu flat black `#10120F` slab with a layered warm obsidian gradient and softer shadow.
  * Replaced the non-dashboard provider top bar flat dark rectangle with a rounded translucent gradient menu bar.
  * Softened provider dashboard cards with subtle gold borders and glass blur so the page reads as layered PRIMORA UI rather than black blocks.
* **Files Changed**:
  * `web_platform/src/app/provider/dashboard/page.tsx`
  * `web_platform/src/app/provider/layout.tsx`
* **Verification**:
  * In-app browser smoke on `/provider/dashboard` confirmed zero large hard-black dashboard/menu rectangles detected.
  * In-app browser smoke on `/provider/settings` confirmed the top menu bar has a rounded gradient background, not a hard black header.
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run test:security-core` passed.
  * `npm run test:admin-controls` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed, with CRLF warnings only.
* **Product Commit**: `ea2bbd7`
* **Next Steps**:
  * Once GitHub connectivity is stable, re-run `git pull origin master` to confirm no upstream coordination changes landed while the network was unavailable.
  * Continue browser-based visual QA for provider subpages to catch any remaining flat dark blocks or overly harsh menu surfaces.

---

### [2026-06-23] Codex - Provider Readability and Token Consistency
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Built on the two local unpushed provider rectangle commits because GitHub HTTPS connectivity was already timing out.
  * Project-local PRIMORA skill files were still absent, so this followed `AGENTS.md` PRIMORA rules and the established provider dashboard implementation pattern.
* **Tasks Completed**:
  * Brightened provider sidebar/menu inactive labels, icons, support text, profile meta, search input, language button, and topbar labels.
  * Normalized provider dashboard cards to one readable dark-glass surface with champagne borders instead of mixed slate borders.
  * Replaced muted `#94A3B8`/`#64748B`/`#9CA3AF` dashboard text tokens with readable ivory/slate labels.
  * Changed the primary gold provider CTA from white text to deep-black text for better contrast.
  * Aligned KPI separators, chart grid lines, progress tracks, and nested cards with the same champagne-gold divider system.
  * Added an Arabic-mode label for the "Stable" KPI state.
* **Files Changed**:
  * `web_platform/src/app/provider/dashboard/page.tsx`
  * `web_platform/src/app/provider/layout.tsx`
* **Verification**:
  * In-app browser smoke on `/provider/dashboard` and `/provider/settings` confirmed no mojibake, no old low-contrast provider tokens, and no white-on-gold CTA text.
  * `rg` token scans confirmed old muted dashboard/menu tokens were removed from the touched provider files.
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run test:security-core` passed.
  * `npm run test:admin-controls` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed, with CRLF warnings only.
* **Product Commit**: `857044b`
* **Next Steps**:
  * Push `codex` once GitHub HTTPS connectivity is restored.
  * Continue visual QA on other provider subpages for remaining local token drift.

---

### [2026-06-24] Codex - Provider Dashboard Admin-Token Readability Fix
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Read `AGENTS.md`, `.cursorrules`, and recent coworking changelog entries before editing.
  * Pulled `origin master`; branch was already up to date.
  * Project-local PRIMORA skill files and the project-local impeccable context loader were not present, so this pass used the available repo rules, the installed product UI guidance, and direct admin/provider dashboard comparison.
* **Tasks Completed**:
  * Removed the provider dashboard's dark beveled outer rectangle by making the page root transparent, borderless, and square to the layout canvas.
  * Replaced provider dashboard dark-glass cards with admin-style light cards, subtle borders, and restrained shadows.
  * Aligned provider dashboard text colors to the admin dashboard palette: `#101828` primary text, `#667085` labels, gray secondary text, and champagne gold accents.
  * Recolored search, language toggle, KPI labels, chart grid lines, nested operation cards, service progress tracks, and prayer-control widgets for readable light-surface contrast.
* **Files Changed**:
  * `web_platform/src/app/provider/dashboard/page.tsx`
* **Verification**:
  * In-app browser reached `/provider/dashboard` after the secure-session guard and confirmed the page root has transparent background, no border, no radius, and admin-matching text colors.
  * In-app browser computed-style checks found no old provider dark-dashboard tokens in the main dashboard DOM.
  * Browser screenshot capture timed out, but DOM/style verification and production build both succeeded.
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed, with CRLF warnings only.
* **Product Commit**: `19eef3c`
* **Next Steps**:
  * Re-run visual QA with an authenticated browser session if the user still sees any low-contrast provider subpage surfaces.
  * Continue syncing provider subpage design tokens with the admin/customer dashboard system where dark legacy panels remain.

---

### [2026-06-25] Codex - Provider Header Line 340 Light Theme Fix
* **Branch/Coordination**:
  * Verified active branch is `codex`; did not switch branches.
  * Read `AGENTS.md`, `.cursorrules`, and the recent coworking changelog before editing.
  * Pulled `origin master`; branch was already up to date.
  * Project-local PRIMORA skill files referenced by `AGENTS.md` were not present, so this used the installed product UI guidance and direct admin/provider token comparison.
* **Tasks Completed**:
  * Replaced the provider layout header at line 340 from a dark gradient/glass menu bar to a white admin-style surface with `#ECECEC` border and restrained shadow.
  * Updated the provider topbar search, language switcher, and profile text to readable admin-dashboard colors: `#101828`, `#667085`, white surfaces, and gold focus/hover accents.
* **Files Changed**:
  * `web_platform/src/app/provider/layout.tsx`
* **Verification**:
  * In-app browser style check on `/provider/settings` confirmed the visible provider header is white, uses a light border, has dark readable input/profile text, and contains zero old dark header tokens.
  * `npx tsc --noEmit --project web_platform/tsconfig.json` passed.
  * `npm run typecheck:mobile` passed.
  * `npm run build --workspace=web_platform` passed.
  * `git diff --check` passed, with CRLF warnings only.
* **Product Commit**: `1af9a4d`
* **Next Steps**:
  * Continue route-by-route provider visual QA for any remaining dark legacy surfaces outside the sidebar.

---

### [2026-06-25] Claude Code - Full button/link connectivity pass
* **Starting point**: latest combined work (Codex tip `5b671c1`); `claude-code` aligned to it in place, tree untouched.
* **Tasks Completed**:
  * Audited every internal link vs. real routes across the whole web app (76 targets) and every `<button>` without a handler across all admin/provider/customer pages.
  * **Admin dashboard**: Quick Actions grid (4 dead buttons) → real destinations (providers / services / coupons / notifications); Reports grid (4 dead) → reports & payout ledger; header bell → `/admin/activity`; "View System Status" → `/admin/system-logs`; sidebar profile kebab → working **sign-out**.
  * **Created 3 missing admin pages** (dashboard linked to them but they didn't exist): `/admin/activity` (live Supabase platform feed + filters), `/admin/help` (support channels + FAQ), `/admin/notifications` (broadcast composer with validation + history). All bilingual EN/AR + RTL, matching the light-lux admin card language.
  * **Customer**: dashboard bell → `/customer/notifications`; messages "View Details" → `/customer/bookings`; packages CTA → `/customer/book`.
  * **Provider**: messages "Client Profile" → `/provider/customers`; pricing page's three plan CTAs now actually select the plan (were fully dead).
  * Verified: `npm run build` passes (EXIT 0, all new routes compiled); all 9 touched routes serve 200 on the running dev server with zero console/compile errors; no hardcoded secrets found.
* **Known decorative controls left (documented, not faked)**: chat attach/emoji buttons (need Supabase Storage bucket), provider calendar Day/Week pill (needs week grid implementation).
* **Files Changed**: `web_platform/src/app/admin/{page,layout}.tsx`, new `admin/{activity,help,notifications}/page.tsx`, `customer/{dashboard,messages,packages}/page.tsx`, `provider/{messages,pricing}/page.tsx`
* **Active Working Commits**: `9ac28a8`, `5764f1c` (branch `claude-code`, merged to `master`)
* **Next Steps**:
  * Wire chat attachments once a storage bucket is provisioned; implement calendar week view.
  * Continue with provider/customer subpage deep audits (forms, filters) and booking-flow E2E test.

---

### [2026-07-03] Claude Code - Phase 2 Master Build (control plane core)
* **Starting point**: latest combined work (`61541e9`, = master = origin); tree clean.
* **Tasks Completed**:
  * **Data model (§4)** — migration `20260703014433_phase2_admin_control_plane.sql`: services gain slug/images/tags/add_ons/status(draft·active·archived, trigger syncs legacy `is_active`)/featured_on_landing/featured_in_services/sort_order + nullable provider_id for the admin-owned catalog; categories gain icon/sort_order; NEW `payment_methods` (KSA registry: mada, Apple Pay, Visa, MC, STC Pay, Tamara, Tabby, wallet, bank transfer, cash) and `integrations` + `integration_audit_log` tables with admin-only RLS; idempotent seeds for the §3 catalog (6 categories, 21 bilingual services with add-ons and featured picks). **⚠ NOT yet applied to the linked Supabase project — CLI token expired (someone must run `npx supabase login`, then `npx supabase db push --linked`).** All UI reads have catalog-shaped fallbacks until then.
  * **Discover → Services rename (§3)** — NEW dynamic `/services` (sticky filters: search, category chips, price band, home-service toggle, sort; uniform cards; detail drawer with add-ons; EN/AR + RTL). `/store` and `/discover` now redirect to it; all links/labels renamed.
  * **Landing featured rail (§2)** — "Featured Services" rail now renders `featured_on_landing` services in admin `sort_order` (hardcoded cards remain only as fallback).
  * **Admin control plane (§5)** — admin/services: featured Landing/Services toggles per row; admin/payments: payment-methods registry (enable/disable/default/roles/env); admin/integrations: rewritten as the full registry (status, enable, test/live, masked keys, webhook, audit log).
  * **Dynamic linkage (§6)** — customer/book "We accept" strip reads enabled customer methods from the registry.
  * Verified: build + TypeScript pass (EXIT 0); all 8 touched routes serve 200 on the running dev server; no new console errors.
* **Files Changed**: new migration; new `app/services/page.tsx`; `store`, `discover`, `page` (landing), `become-provider`, `service-board`, `shop/[id]`, `admin/{services,payments,integrations}`, `customer/book`.
* **Active Working Commits**: `310ad7c`, `14ceb2f`, `93a811e`, `16547b0` (branch `claude-code`, merged to `master`)
* **Next Steps**:
  * Apply the pending migration once Supabase CLI is re-authenticated, then verify the featured-toggle → landing rail loop live.
  * §1 global container/Card sweep; §5.1 full create/edit drawer + provider assignment UI + category manager; §5.2 provider verification polish; hero/theme work (§2 dark-mode toggle, Fraunces/Inter fonts, `#F2EEE6`/`#C29A4C` palette alignment).

---

### [Template for future entries]
```markdown
### [YYYY-MM-DD] [Model Name]
* **Tasks Completed**:
  * [List files modified & what was changed]
* **Active Working Commit**: [commit-hash]
* **Next Steps**:
  * [Planned modifications]
```
